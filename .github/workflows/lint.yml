name: lint

on:
  pull_request:
    paths:
      - '**/*.py'

jobs:
  # mypy:
  #   name: mypy
  #   runs-on: ubuntu-latest
  #   defaults:
  #     run:
  #       working-directory: backend
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4
  #     - name: install uv
  #       uses: astral-sh/setup-uv@v3
  #       with:
  #         version: "latest"
  #         enable-cache: true
  #         cache-dependency-glob: "backend/uv.lock"
  #     - name: Set up Python
  #       run: uv python install 3.12
  #     - name: Install dependencies
  #       run: uv sync --frozen
  #     - name: Run mypy
  #       run: uv run mypy . --strict
  #     - name: Run mypy with annotation
  #       run: |
  #         uv run mypy . --output=github

  ruff:
    name: ruff
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          version: "latest"
          enable-cache: true
          cache-dependency-glob: "backend/uv.lock"
      - name: Set up Python
        run: uv python install 3.12
      - name: Install Ruff
        run: uv tool install ruff
      - name: Run Ruff lint
        run: ruff check . --output-format=github
      - name: Run ruff formatter check
        run: ruff format . --check

  test:
    name: Test
    runs-on: ubuntu-latest
    needs: ruff
    defaults:
      run:
        working-directory: backend
    
    services:
      redis:
          image: redis:7
          options: >-
            --health-cmd "redis-cli ping"
            --health-interval 10s
          ports:
            - 6379:6379
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: testpass
          POSTGRES_DB: testdb
        options: --health-cmd pg_isready --health-interval 10s
        ports:
          - 5432:5432
    
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v3
      
      - name: Install dependencies
        run: uv sync --frozen
      
      - name: Run migrations
        run: uv run python manage.py migrate --noinput
        env:
          DJANGO_SETTINGS_MODULE: config.settings.common
          DATABASE_URL: postgres://postgres:testpass@localhost/testdb
      
      - name: Run your exact command
        run: |
          uv run python manage.py test apps.api.tests.unit --failfast --noinput
        env:
          DJANGO_SETTINGS_MODULE: config.settings.common
          REDIS_URL: redis://localhost:6379
          CELERY_BROKER_URL: redis://localhost:6379
          DATABASE_URL: postgres://postgres:testpass@localhost/testdb